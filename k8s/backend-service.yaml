apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: weekly-report
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: weekly-report-system
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
